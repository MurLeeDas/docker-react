sudo: required
services:
  - docker

before_install:
  - docker build -t MurLeeDas/docker-react -f Dockerfile.dev .

script:
  - echo "$password" | docker login -u "$username" --password-stdin
  - docker run MurLeeDas/docker-react npm run test -- --coverage


